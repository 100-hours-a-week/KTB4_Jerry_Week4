spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
jwt:
  secret: test-secret-key-for-hmac-sha256-at-least-32-bytes-long-0123456789
app:
  s3:
    bucket: test-bucket